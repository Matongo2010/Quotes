import { VoterDirective } from './voter.directive';

describe('VoterDirective', () => {
  it('should create an instance', () => {
    const directive = new VoterDirective();
    expect(directive).toBeTruthy();
  });
});
